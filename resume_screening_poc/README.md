# App Package

This folder contains the Streamlit app for the public-lite demo.

Use the repository root README for installation, API configuration, and license notes.

Run from this folder:

```powershell
streamlit run app.py
```

The app supports text resumes, PDF/DOCX parsing, and OCR through a configured vision model. OCR results depend on model quality and valid API credentials.
